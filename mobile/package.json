{
  "name": "omnibites-mobile",
  "version": "1.0.0",
  "description": "Wrapper móvil de Omnibites usando Capacitor",
  "private": true,
  "scripts": {
    "prepare-www": "node copy-www.js",
    "sync": "npm run prepare-www && npx cap sync android",
    "open": "npx cap open android",
    "generate-icons": "npx capacitor-assets generate --android",
    "build:apk": "npm run prepare-www && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "@capacitor/filesystem": "^6.0.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2",
    "@capacitor/assets": "^3.0.5"
  }
}
